import React from 'react'
import Debug from 'debug'

const UNITY = 0

const FaderCap = {gain} => (
  //gain
  //
  <div className='fader-cap'>

  </div>
)

const Fader = { name = '' } => (
  <div className='fader'>
    <label>{name}</label>
    <div className='fader-container'>
      <FaderCap =
    </div>
  </div>
)
